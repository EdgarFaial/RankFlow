
// Este arquivo foi removido pois utilizava o driver nativo 'mongodb' (MongoClient), 
// que é incompatível com ambientes de navegador (Vite/React).
// A lógica correta de conexão via Atlas Data API (HTTP) reside em services/mongoDBService.ts.
